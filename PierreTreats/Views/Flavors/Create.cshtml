@{
	Layout = "_Layout";
}

@model PierreTreats.Models.Flavor

<h3>Create a new Flavor</h3>

@using (Html.BeginForm())
{
	@Html.LabelFor(m => m.Name)
	@Html.TextBoxFor(m => m.Name)
	<input type="submit" class="btn btn-primary" value="Add Flavor"/>
}